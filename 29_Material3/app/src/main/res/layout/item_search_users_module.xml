<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="listener"
            type="com.chunchiehliang.material3.ui.search.SearchUsersModuleAdapter.SearchUsersModuleViewHolder.Listener" />
    </data>

    <TextView
        android:id="@+id/tv_search_users"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?android:selectableItemBackground"
        android:drawablePadding="@dimen/material_spacing"
        android:gravity="center_vertical"
        android:minHeight="?minTouchTargetSize"
        android:onClick="@{() -> listener.onClick()}"
        android:paddingHorizontal="@dimen/material_margin"
        android:text="@string/search_for_users"
        app:drawableEndCompat="@drawable/ic_arrow_forward"
        app:drawableStartCompat="@drawable/ic_user"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</layout>